import type { ContextEstimateSummary, ConversationMessage, Decision, ErrorRecord, PhaseId, ProjectMemory, SessionState, TokenBudgetSummary } from './types.js';
export declare class Memory {
    private projectPath;
    private mp;
    private legacyMp;
    constructor(projectPath: string);
    private read;
    private write;
    private hubPath;
    private defaultTokenUsage;
    private normalizeSession;
    private getBudgetLevel;
    private getBudgetStatus;
    private buildBudgetAdvice;
    private roughTokenCount;
    private fileTokenCount;
    private resolveEstimateFiles;
    private buildEstimateAdvice;
    private summarizeTokenUsage;
    estimateContextPressure(taskHint?: string): ContextEstimateSummary | null;
    private preview;
    private syncStatusFile;
    private appendSessionLogEntry;
    private syncDecisionsFile;
    private syncTokenFile;
    getProject(): ProjectMemory | null;
    saveProject(p: ProjectMemory): void;
    initProject(name: string, desc: string): ProjectMemory;
    getDecisions(): Decision[];
    addDecision(d: Omit<Decision, 'id' | 'timestamp'>): Decision;
    getErrors(): ErrorRecord[];
    addError(e: Omit<ErrorRecord, 'id' | 'timestamp'>): ErrorRecord;
    getPreventionRules(): string[];
    getSession(): SessionState | null;
    getTokenSummary(): TokenBudgetSummary | null;
    getContextEstimate(taskHint?: string): ContextEstimateSummary | null;
    refreshProjectHub(taskHint?: string): void;
    saveSession(s: SessionState): void;
    initSession(): SessionState;
    recordTokenUsage(agentId: string, tokensUsed: number, model: string): TokenBudgetSummary;
    addMessage(m: Omit<ConversationMessage, 'id' | 'timestamp'>): void;
    setActiveAgent(id: string): void;
    setPhase(p: PhaseId): void;
    setGate(g: string): void;
    buildContext(): string;
}
